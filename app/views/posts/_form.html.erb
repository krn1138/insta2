<%= form_with(model: @post, local: true ,url: choose_new_or_edit ) do |form| %>
      <div class="blog_title">
      <%= form.label :image %>
      <%= form.file_field :iamge %>
    </div>
    <div class="blog_content">
      <%= form.label :content %>
      <%= form.text_field :content %>
    </div>
    <%= form.submit %>
  <% end %>
